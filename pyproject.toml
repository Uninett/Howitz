[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "howitz"
description = "blbl"
authors = [
    {name = "Hanne Moa", email = ""},
    {name = "Ilona Podliashanyk", email = ""},
]
readme = "README.rst"
classifiers = [
    "License :: OSI Approved :: MIT License",
]
requires-python = ">=3.8"
dynamic = ["version"]
dependencies = [
    "flask",
]
